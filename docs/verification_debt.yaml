# filepath: docs/verification_debt.yaml
# The agent adds entries; only the human closes them.
# Schema per masterplan §1.3:
# - id: VD-001
#   package: wp-x
#   claim_untested: "..."
#   original_criterion: "..."
#   discharge_path: "..."
#   status: OPEN
debts:
  - id: VD-001
    package: wp-g
    claim_untested: "systemd boot ordering (clocks -> xrce -> perception -> autonomy), Restart=on-failure recovery, and journald rate limiting under real boot"
    original_criterion: "components activate alongside their WPs' tokens"
    discharge_path: "install+enable units at gate1/gate5 integration sessions; observe one full boot cycle and one induced crash per service"
    status: OPEN
