<!-- filepath: src/livox_custommsg_adapter/README.md -->
# livox_custommsg_adapter — DEFERRED

Reserved location for the sim-side PointCloud2 -> livox CustomMsg adapter the
SITL rig will need (FAST-LIO requires CustomMsg per-point timestamps; no
simulator emits them natively). Intentionally empty until the sim phase.
